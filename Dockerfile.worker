FROM node:20-alpine

WORKDIR /app

# 安装依赖
COPY package*.json ./
# alpine/npm 小版本差异时 ci 可能因 optional 依赖不同步；回退 install
RUN npm ci || npm install

# 复制源代码
COPY . .

# 生成 Prisma Client
RUN npx prisma generate

# 启动 Worker
CMD ["npm", "run", "worker:email"]
